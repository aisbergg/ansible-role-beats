---
__heartbeat_config_defaults:
  heartbeat.config.monitors:
    path: ${path.config}/monitors.d/*.yml
    reload.enabled: false
    reload.period: 5s
  heartbeat.monitors: []
  logging.to_files: false
